{
  "name": "mlbtv-ad-muter",
  "version": "0.2.2",
  "private": true,
  "description": "Locally detects commercial breaks on MLB.TV and can mute the browser tab while commercials continue playing.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "npm run build && npm run typecheck:tests && node --test",
    "typecheck:tests": "tsc --project tsconfig.test.json",
    "package:release": "sh scripts/package-release.sh"
  },
  "engines": {
    "node": ">=22.18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.20.1",
    "chrome-types": "^0.1.436",
    "typescript": "^7.0.2"
  }
}
