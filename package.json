{
  "name": "mlbtv-ad-muter",
  "version": "0.2.0",
  "private": true,
  "description": "A local-only Chrome extension that detects commercial-break player states and mutes the tab.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "npm run build && node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "devDependencies": {
    "chrome-types": "^0.1.436",
    "typescript": "^7.0.2"
  }
}
